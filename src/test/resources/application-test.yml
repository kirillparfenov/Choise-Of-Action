server:
  port: 8081
  servlet:
    context-path: /api

spring:
  application:
    name: choise-of-action
  datasource:
    url: jdbc:postgresql://localhost:5433/postgres?currentSchema=public
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver